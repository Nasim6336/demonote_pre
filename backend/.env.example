# MongoDB connection string (Atlas recommended for Render)
MONGO_URI=your_mongodb_connection_string
DB_PASSWORD=
# Secret for JWT signing
JWT_SECRET=your_jwt_secret

# Optional: Frontend URL for CORS (e.g., https://your-app.vercel.app)
FRONTEND_URL=

# Port for local development (Render provides this automatically in production)
PORT=4000
